import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: "90%",
        backgroundColor: "#fff",
        gap: 5,
    },
    input: {
        borderWidth: 1,
        borderStyle: "solid",
        borderColor: "black",
        borderRadius: 5,
        padding: 5
    },
    title: {
        fontWeight: "500",
    }
});
